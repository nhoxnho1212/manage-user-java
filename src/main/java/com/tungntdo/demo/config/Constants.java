package com.tungntdo.demo.config;

public class Constants {
    public interface URL {
        interface USER {
            String MAIN = "/users";
            String LOGIN = MAIN + "/login";
        }
    }
}
